# Prep Plan: Moveworks — Senior Software Engineer
**Interview:** 2026-05-11
**Rounds:** DSA
**Generated:** 2026-05-08

---

## Day 1 — String DP & Word Breaking

### DSA: Word Break II (LeetCode #140)
- **Pattern / goal:** Backtracking + memoization on string partitioning; explore all valid dictionary splits and return them.
- **Approach:**
  - Recognize this is a *generate all solutions* problem, not just yes/no (like Word Break I).
  - Build recursively: for each prefix in the dictionary, recurse on the rest and prepend valid combinations.
  - Memoize to avoid recomputing the same substring partitions.
  - Trace through a concrete example: `s = "catsandcatsdog"`, `wordDict = ["cat", "cats", "and", "sand", "dog"]` — show how memoization prevents redundant calls.
- **Time:** 60 min

### DSA: Synonymous Sentences (LeetCode #1258)
- **Pattern / goal:** Backtracking + graph (synonym relationships); generate all valid sentence permutations in sorted order using DFS.
- **Approach:**
  - Parse the input sentence and synonym pairs into a graph (adjacency list or union-find to group synonyms).
  - Use DFS to explore all valid word combinations at each position.
  - Sort results lexicographically and return.
  - Focus on the harder variant: handle multiple synonym chains and cross-dependencies; trace through `text = "this apple is sweet"`, `synonyms = [["apple","fruit"],["sweet","tasty"]]`.
- **Time:** 60 min

---

## Day 2 — Dynamic Programming & String Manipulation

### DSA: Regular Expression Matching (LeetCode #10)
- **Pattern / goal:** 2D DP on strings with wildcard matching (`.` and `*`); classic pattern-matching problem.
- **Approach:**
  - Build a DP table where `dp[i][j]` = true if the first `i` characters of string match the first `j` characters of pattern.
  - Handle the `*` metacharacter carefully: it means zero or more of the preceding character.
  - Write out the recurrence explicitly and trace through edge cases (e.g., `"aa"` vs `"a*"`, `"ab"` vs `".*"`).
  - Optimize space if time permits.
- **Time:** 60 min

### DSA: Shortest Uncommon Substring in an Array (LeetCode #1534)
- **Pattern / goal:** Brute-force string search + optimization; for each string find the shortest substring not present in any other string.
- **Approach:**
  - Naive: for each string, generate all substrings, check if each appears in any other string.
  - Optimize by sorting by substring length (shortest first) and breaking early.
  - Use a set of all substrings from other strings for O(1) lookup.
  - Trace through `strs = ["aaaa","asasa","aa"]` — verify that "asasa" returns "asas", "aa" returns "a".
- **Time:** 60 min

---

## Day 3 — Arrays, Hashing & Expression Evaluation

### DSA: Subarray with Target Sum (HashMap / Prefix Sum)
- **Pattern / goal:** Prefix sum + hash map; detect subarrays with a given sum in linear time.
- **Approach:**
  - Use a hash map to store cumulative sum → count/index.
  - For each element, check if `current_sum - target` exists in the map (meaning a subarray sums to target).
  - Handle zero-sum edge cases carefully.
  - Trace through `arr = [1, -1, 3, 2]`, `target = 4` — show how prefix sums yield the subarray `[3, 1]`.
- **Time:** 60 min

### DSA: Evaluate Mathematical Expression (Stack / Postfix Evaluation)
- **Pattern / goal:** Stack-based expression evaluation with operator precedence and parentheses; convert to postfix or use two-stack algorithm.
- **Approach:**
  - Parse an infix expression respecting BODMAS (Brackets, Division/Multiplication, Addition/Subtraction).
  - Use the Shunting Yard algorithm or dual-stack approach.
  - Handle parentheses, negative numbers, and multi-digit operands.
  - Trace through `"3 + 2 * 5 - (10 / 2)"` → expected output `8`.
- **Time:** 60 min

---

## Weak areas to revisit
None identified yet — first session will surface these.